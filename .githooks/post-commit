#!/bin/sh
set -e

# Keep the local release-planning ledger current after every completed commit.
# plan/ is intentionally gitignored, so this does not create a bookkeeping
# commit or alter the commit that has just been created.
node scripts/update-local-updates.js --quiet
